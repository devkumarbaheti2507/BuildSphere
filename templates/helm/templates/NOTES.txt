{{- if .Values.ingress.enabled }}
The application is available through these ingress hosts:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{- end }}://{{ .host }}{{ (index .paths 0).path }}
{{- end }}
{{- else }}
Run these commands to access the application locally:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "{{serviceName}}.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward "$POD_NAME" 8080:{{ .Values.service.targetPort }}
{{- end }}
